{
  "name": "reclama-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Generador privado y guiado de reclamaciones para consumidores.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "ci": "npm run check && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
